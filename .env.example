# Copy to .env to run the live integration test locally. Both are optional; without a token the
# live test is skipped. Loaded by `npm test` via Node's --env-file-if-exists. Never commit .env.
YNAB_ACCESS_TOKEN=
YNAB_BUDGET_ID=
